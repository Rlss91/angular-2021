import { UpperriversPipe } from './upperrivers.pipe';

describe('UpperriversPipe', () => {
  it('create an instance', () => {
    const pipe = new UpperriversPipe();
    expect(pipe).toBeTruthy();
  });
});

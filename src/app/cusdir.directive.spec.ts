import { CusdirDirective } from './cusdir.directive';

describe('CusdirDirective', () => {
  it('should create an instance', () => {
    const directive = new CusdirDirective();
    expect(directive).toBeTruthy();
  });
});
